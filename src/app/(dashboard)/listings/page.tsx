import { Briefcase } from "lucide-react";

import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function ListingsPage() {
  return (
    <PagePlaceholder
      icon={Briefcase}
      title="Listings"
      description="This section is out of scope for the assessment. Listing management will live here."
    />
  );
}
