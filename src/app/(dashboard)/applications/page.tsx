import { ClipboardCheck } from "lucide-react";

import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function ApplicationsPage() {
  return (
    <PagePlaceholder
      icon={ClipboardCheck}
      title="Applications"
      description="This section is out of scope for the assessment. Applications will live here."
    />
  );
}
